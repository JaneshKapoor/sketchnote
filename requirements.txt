# --- Sketchnote: Hugging Face Space (Gradio SDK) dependencies ---
# Light/in-Space work only. Heavy models (Nemotron Parse, MiniCPM, SDXL-Turbo)
# live in the Modal image (see modal_app.py), NOT here.

gradio==6.18.0
pymupdf>=1.24
kokoro>=0.9.2
soundfile>=0.12
numpy>=1.26,<2.3
opencv-python-headless>=4.9
python-dotenv>=1.0
modal>=0.64
cairosvg>=2.7
matplotlib>=3.8
Pillow>=10.2

# ffmpeg binary fallback for environments without a system ffmpeg
# (the Space provides ffmpeg via packages.txt; this is a safety net).
imageio-ffmpeg>=0.5

# Optional in-Space LLM fallback (escape hatch in Section 4). Uncomment to run
# MiniCPM in-Space in 4-bit instead of on Modal:
# transformers>=4.44
# torch>=2.2
# accelerate>=0.33
# bitsandbytes>=0.43
